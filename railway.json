{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "echo building!"
    },
    "deploy": {
        "preDeployCommand": ["./out"],
        "startCommand": "echo starting!",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 100,
        "restartPolicyType": "never"
    }
}
